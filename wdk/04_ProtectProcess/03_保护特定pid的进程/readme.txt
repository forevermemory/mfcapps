编译时候需要加上选项,配置如下
项目 -- 属性 -- 链接器 -- 命令行 -- 其它选项
	/INTEGRITYCHECK 

测试环境:
	win10  1903 (18362.30)

实验流程:

	通过r3的MFC程序将需要保护的pid传入r0中，r0将pid放到数组中，当触发跨进程操作时候，判断被操作的进程是否在列表中。
	

测试结果:
	可以达到保护指定pid的进程，ce读取不到被保护进程的数据，任务管理器不能结束改进程
	

	